---js
{
  layout: "layouts/page.njk",
  title: "Zaštita okoliša i upravljanje otpadom",
  description: "Zaštita okoliša i upravljanje otpadom",
  banner: "blog.jpg",
  navID: "blog-zouo",
  permalink: "blog/zastita-okolisa-i-upravljanje-otpadom{% if pagination.pageNumber > 0 %}/stranica/{{ pagination.pageNumber + 1}}{% endif %}/index.html",
  pagination: {
    data: "objave.sve",
    size: 5,
	  alias: "objave",
    before: function(data) {
      const sviobjekti = [];
	  
      for (const obj of data) {
        if ( obj.categoryslug === "zastita-okolisa-i-upravljanje-otpadom" ) {
            sviobjekti.push(obj);
        }
	    }
      if (!sviobjekti.length) {
        sviobjekti.push("prazno")
      }

      return sviobjekti;
    }
  },
  eleventyComputed: {
    page_title: "Blog - Zaštita okoliša i upravljanje otpadom {% if pagination.pages | length != 1 %}({{ pagination.pageNumber + 1 }}/{{ pagination.pages | length }}){% endif %}",
    page_description: "Blog - Zaštita okoliša i upravljanje otpadom {% if pagination.pages | length != 1 %}({{ pagination.pageNumber + 1 }}/{{ pagination.pages | length }}){% endif %}"
  }
}
---

{% include "partials/blog-header.njk" %}

{% include "partials/blog-category.njk" %}