---js
{
  layout: "layouts/page.njk",
  title: "Upravljanje ljudskim potencijalima",
  description: "Upravljanje ljudskim potencijalima",
  banner: "blog.jpg",
  navID: "blog-uljp",
  permalink: "blog/upravljanje-ljudskim-potencijalima{% if pagination.pageNumber > 0 %}/stranica/{{ pagination.pageNumber + 1}}{% endif %}/index.html",
  pagination: {
    data: "objave.sve",
    size: 5,
	  alias: "objave",
    before: function(data) {
      const sviobjekti = [];
	  
      for (const obj of data) {
        if ( obj.categoryslug === "upravljanje-ljudskim-potencijalima" ) {
            sviobjekti.push(obj);
        }
	    }
      if (!sviobjekti.length) {
        sviobjekti.push("prazno")
      }

      return sviobjekti;
    }
  },
  eleventyComputed: {
    page_title: "Blog - Upravljanje ljudskim potencijalima {% if pagination.pages | length != 1 %}({{ pagination.pageNumber + 1 }}/{{ pagination.pages | length }}){% endif %}",
    page_description: "Blog - Upravljanje ljudskim potencijalima {% if pagination.pages | length != 1 %}({{ pagination.pageNumber + 1 }}/{{ pagination.pages | length }}){% endif %}"
  }
}
---

{% include "partials/blog-header.njk" %}

{% include "partials/blog-category.njk" %}