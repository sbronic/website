---js
{
  layout: "layouts/page.njk",
  title: "Financije i računovodstvo",
  description: "Financije i računovodstvo",
  banner: "blog.jpg",
  navID: "blog-fir",
  permalink: "blog/financije-i-racunovodstvo{% if pagination.pageNumber > 0 %}/stranica/{{ pagination.pageNumber + 1}}{% endif %}/index.html",
  pagination: {
    data: "objave.sve",
    size: 5,
	  alias: "objave",
    before: function(data) {
      const sviobjekti = [];
	  
      for (const obj of data) {
        if ( obj.categoryslug === "financije-i-racunovodstvo" ) {
            sviobjekti.push(obj);
        }
	    }
      if (!sviobjekti.length) {
        sviobjekti.push("prazno")
      }

      return sviobjekti;
    }
  },
  eleventyComputed: {
    page_title: "Blog - Financije i računovodstvo {% if pagination.pages | length != 1 %}({{ pagination.pageNumber + 1 }}/{{ pagination.pages | length }}){% endif %}",
    page_description: "Blog - Financije i računovodstvo {% if pagination.pages | length != 1 %}({{ pagination.pageNumber + 1 }}/{{ pagination.pages | length }}){% endif %}"
  }
}
---

{% include "partials/blog-header.njk" %}

{% include "partials/blog-category.njk" %}