---
title: On-line edukacije
description: Informatika i uredsko poslovanje
layout: layouts/page.njk
banner: online-edukacije.jpg
navID: online-iup
pagination:
  data: online.iup
  size: 10
  alias: edukacije
permalink: "edukacije/online-edukacije/informatika-i-uredsko-poslovanje{% if pagination.pageNumber > 0 %}/page{{ pagination.pageNumber + 1}}{% endif %}/index.html"
eleventyComputed:
    page_title: "On-line edukacije - informatika i uredsko poslovanje {% if pagination.pages | length != 1 %}({{ pagination.pageNumber + 1 }}/{{ pagination.pages | length }}){% endif %}"
    page_description: "On-line edukacije - informatika i uredsko poslovanje {% if pagination.pages | length != 1 %}({{ pagination.pageNumber + 1 }}/{{ pagination.pages | length }}){% endif %}"
---

{% include "partials/online-header.njk" %}

{% include "partials/online-category.njk" %}