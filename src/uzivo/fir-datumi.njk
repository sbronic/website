---js
{
  layout: "layouts/page.njk",
  title: "Seminari uživo",
  description: "Financije i računovodstvo",
  banner: "seminari.jpg",
  navID: "sem-fir",
  nav2ID: "datumi",
  permalink: "edukacije/seminari-uzivo/financije-i-racunovodstvo/datumi-odrzavanja{% if pagination.pageNumber > 0 %}/stranica/{{ pagination.pageNumber + 1}}{% endif %}/index.html",
  pagination: {
    data: "seminari-uzivo.datumi",
    size: 10,
	  alias: "seminari",
    before: function(data) {
      const sviobjekti = [];
	  
      for (const obj of data) {
        if ( obj.kategorijaslug === "financije-i-racunovodstvo" ) {
            sviobjekti.push(obj);
        }
	    }
      if (!sviobjekti.length) {
        sviobjekti.push("prazno")
      }

      return sviobjekti;
    }
  },
  eleventyComputed: {
    page_title: "Datumi održavanja seminara - Financije i računovodstvo {% if pagination.pages | length != 1 %}({{ pagination.pageNumber + 1 }}/{{ pagination.pages | length }}){% endif %}",
    page_description: "Datumi održavanja seminara - Financije i računovodstvo {% if pagination.pages | length != 1 %}({{ pagination.pageNumber + 1 }}/{{ pagination.pages | length }}){% endif %}"
  }
}
---

{% include "partials/uzivo-datumi-header.njk" %}

{% include "partials/uzivo-datumi.njk" %}