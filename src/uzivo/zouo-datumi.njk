---js
{
  layout: "layouts/page.njk",
  title: "Seminari uživo",
  description: "Zaštita okoliša i upravljanje otpadom",
  banner: "seminari.jpg",
  navID: "sem-zouo",
  nav2ID: "datumi",
  permalink: "edukacije/seminari-uzivo/zastita-okolisa-i-upravljanje-otpadom/datumi-odrzavanja{% if pagination.pageNumber > 0 %}/stranica/{{ pagination.pageNumber + 1}}{% endif %}/index.html",
  pagination: {
    data: "seminari-uzivo.datumi",
    size: 10,
	  alias: "seminari",
    before: function(data) {
      const sviobjekti = [];
	  
      for (const obj of data) {
        if ( obj.kategorijaslug === "zastita-okolisa-i-upravljanje-otpadom" ) {
            sviobjekti.push(obj);
        }
	    }
      if (!sviobjekti.length) {
        sviobjekti.push("prazno")
      }

      return sviobjekti;
    }
  },
  eleventyComputed: {
    page_title: "Datumi održavanja seminara - Zaštita okoliša i upravljanje otpadom {% if pagination.pages | length != 1 %}({{ pagination.pageNumber + 1 }}/{{ pagination.pages | length }}){% endif %}",
    page_description: "Datumi održavanja seminara - Zaštita okoliša i upravljanje otpadom {% if pagination.pages | length != 1 %}({{ pagination.pageNumber + 1 }}/{{ pagination.pages | length }}){% endif %}"
  }
}
---

{% include "partials/uzivo-datumi-header.njk" %}

{% include "partials/uzivo-datumi.njk" %}